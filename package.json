{
  "name": "rallyos-hub",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "prepare": "husky install",
    "setup": "pnpm install && pnpm run prepare"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^14.0.1"
  }
}
