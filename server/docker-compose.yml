services:
  hub:
    build: .
    container_name: rallyOS-hub
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
    restart: always
